{% extends 'base_explorer.html.twig' %}

{% block title %}User{% endblock %}

{% block body %}
{% block stylesheets %}
 <link href="{{asset('assets/css/core.css') }}" rel="stylesheet" type="text/css" />
{% endblock %}
    <h1>Utilisateur :</h1>
    <div class="m-t-40 card-box">
        <div class="table-responsive">
            <table class="table">
                <tbody>
                
                    <tr>
                        <th>Email</th>
                        <td>{{ user.email }}</td>
                    </tr>
                    <tr>
                        <th>Nom</th>
                        <td>{{ user.username }}</td>
                    </tr>
                    <tr>
                        <th>Rôle</th>
                        <td>{{ user.roles ? user.roles|join(', ') : '' }}</td>
                    </tr>
                
                    <tr>
                        <th>Image</th>
                        <td>{{ user.image }}</td>
                    </tr>
                    <tr>
                        <th>Date d'inscription</th>
                        <td>{{ user.createdAt ? user.createdAt|date('d-m-Y H:i:s') : '' }}</td>
                    </tr>
                </tbody>
            </table>
        </div>
    </div>
    <a href="{{ path('user_index') }}" style="color:grey";>Retour</a> |

    <a href="{{ path('user_edit', {'id': user.id}) }}" style="color:grey";>Modifier</a>

    {{ include('user/_delete_form.html.twig') }}
{% endblock %}

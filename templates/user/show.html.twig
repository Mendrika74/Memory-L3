{% extends 'base_explorer.html.twig' %}

{% block title %}User{% endblock %}

{% block body %}
{% block stylesheets %}
 <link href="{{asset('assets/css/core.css') }}" rel="stylesheet" type="text/css" />
{% endblock %}
    <h1>User</h1>
    <div class="table-responsive">
        <table class="table">
            <tbody>
               
                <tr>
                    <th>Email</th>
                    <td>{{ user.email }}</td>
                </tr>
                <tr>
                    <th>Nom</th>
                    <td>{{ user.username }}</td>
                </tr>
                <tr>
                    <th>Rôle</th>
                    <td>{{ user.roles ? user.roles|join(', ') : '' }}</td>
                </tr>
               
                <tr>
                    <th>Image</th>
                    <td>{{ user.image }}</td>
                </tr>
                <tr>
                    <th>Date d'inscription</th>
                    <td>{{ user.createdAt ? user.createdAt|date('d-m-Y H:i:s') : '' }}</td>
                </tr>
            </tbody>
        </table>
    </div>
    <a href="{{ path('user_index') }}" style="color:grey";>back to list</a> |

    <a href="{{ path('user_edit', {'id': user.id}) }}" style="color:grey";>edit</a>

    {{ include('user/_delete_form.html.twig') }}
{% endblock %}
